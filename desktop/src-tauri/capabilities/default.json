{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The splash window: core APIs so it can ask Rust where the daemon is.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
